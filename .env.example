# Required
OPENAI_APIKEY=?
ANTHROPIC_APIKEY=?

# Optional model overrides (see src/lib/ai.ts for allowed values)
# AX_GENERATE_MODEL=claude-sonnet-4-20250514
# AX_VALIDATE_MODEL=gpt-4o-mini

# Loop budget
AX_MAX_ITERS=4
AX_WORKER_MAX_STEPS=80
AX_WORKER_MAX_LLM_CALLS=60
AX_PROGRESS_HEARTBEAT_MS=8000

# Output directory (traces)
AX_OUT_DIR=out
