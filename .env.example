# Required
OPENAI_APIKEY=?
ANTHROPIC_APIKEY=?

# Optional model overrides (see src/lib/ai.ts for allowed values)
# GENERATE_MODEL=claude-sonnet-4-20250514
# VALIDATE_MODEL=gpt-4o-mini

# Loop budget
MAX_ITERS=4
WORKER_MAX_STEPS=80
WORKER_MAX_LLM_CALLS=60
PROGRESS_HEARTBEAT_MS=8000

# Output directory (traces)
OUT_DIR=out
